<h1>Tasks</h1>

<ol>
<% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task) %> <%= link_to "destroy", task_path(task), method: :delete, data: { confirm: "Are you sure?" } %> </li>
<% end %>
</ol>

<%= link_to "Create new task", :new_task %>
